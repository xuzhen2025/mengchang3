# 原型设计执行规则

## 固定执行顺序

1. 收到需求后，先检查平台内是否已经存在相同或相近的页面、功能、组件、按钮和弹框。
2. 对于相同概念或相同名称的功能，默认直接复用已有设计，包括名称、入口位置、页面结构、视觉样式、按钮样式、交互流程、弹框内容和状态反馈。
3. 在复用基础上，只识别当前端或当前业务确实需要的数据差异，不把身份、端侧或使用场景差异自行扩展为新的页面结构或功能模块。
4. 如果需求中存在任何未明确的范围、分类、业务规则、权限、入口、结果页或交互差异，先整理成具体问题向用户确认，等待确认后再设计或修改。
5. 未经用户明确要求，不自行新增分类、入口、功能、说明、权限模块或管理能力。
6. 确认需求后再实施，并检查入口、操作过程、结果反馈、详情页和异常状态是否闭环，原型案例是否充足且数据一致。
7. 完成后对照已有页面或用户提供的参考图进行视觉与交互检查，确认没有影响其他已复用组件。

## 长期设计要求

1. 每次设计需要保持平台风格一致。如果涉及相同功能或组件，能复用的尽量复用。例如两个按钮名称相同，需要检查按钮样式以及点击后的交互或弹框是否可以复用；不确定时先询问。
2. 由于设计的是原型，需要考虑功能是否能够闭环、案例是否充足、样例数据是否合理。
3. 如果提供了图片或截图，尽量严格按照图片内容、信息层级和排版进行设计。除非用户提出特别要求，否则以参考图为准；发现图片与现有规则冲突时先询问。
4. 用户要求自行设计时，需要判断功能的实际用途、必要性和误导风险，不添加无法解释用途或无法形成闭环的内容。

